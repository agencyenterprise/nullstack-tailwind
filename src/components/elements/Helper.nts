interface HelperProps {
  class?: string;
  children: string;
}

export default function Helper(props: HelperProps) {
  const { class: klass, children } = props;

  return (
    <p class={"mt-2 text-sm text-gray-500".concat(klass ? ` ${klass}` : "")}>
      {children}
    </p>
  );
}
